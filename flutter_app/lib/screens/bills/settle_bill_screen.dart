import 'package:flutter/material.dart';

class SettleBillScreen extends StatelessWidget {
  const SettleBillScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Settle Bill')),
      body: const Center(child: Text('Settle Bill Screen - Coming Soon')),
    );
  }
}
