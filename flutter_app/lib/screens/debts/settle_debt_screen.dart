import 'package:flutter/material.dart';

class SettleDebtScreen extends StatelessWidget {
  const SettleDebtScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Settle Utang')),
      body: const Center(child: Text('Settle Debt Screen - Coming Soon')),
    );
  }
}
